import type { Metadata } from 'next'

export const metadata: Metadata = {
  alternates: {
    canonical: '/how-property-valuation-works-singapore',
  },
}

export default function HowPropertyValuationWorksSingaporeLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
